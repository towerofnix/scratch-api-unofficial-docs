# `cdn.projects. /` API

The project JSON API retrieves the project's JSON data.

### `GET cdn.projects.scratch.mit.edu/<Project ID>/`

Retrieves the projects JSON data, which is where all of the code for a scratch project is stored.
